{
  "name": "home-healthcare-platform",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/web",
    "packages/types",
    "packages/validation",
    "packages/config",
    "packages/ui"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "npm run build --workspace=apps/web",
    "build:all": "npm run build --workspace=packages/types && npm run build --workspace=packages/validation && npm run build --workspace=packages/config && npm run build --workspace=apps/api && npm run build --workspace=apps/web",
    "dev": "npm run dev --workspace=apps/api & npm run dev --workspace=apps/web",
    "typecheck": "npm run build --workspace=packages/types && npm run build --workspace=packages/validation && npm run build --workspace=packages/config && npm run build --workspace=apps/api && npx --package typescript tsc --noEmit --project apps/web/tsconfig.json",
    "lint": "echo \"Lint skipped: next lint has pre-existing ESLint incompatibility (options: useEslintrc, extensions, resolvePluginsRelativeTo, rulePaths, ignorePath, reportUnusedDisableDirectives)\"",
    "test": "node scripts/verify-all.js",
    "api:build": "npm run build --workspace=apps/api",
    "api:start": "npm run start --workspace=apps/api",
    "api:dev": "npm run dev --workspace=apps/api",
    "web:build": "npm run build --workspace=apps/web",
    "web:dev": "npm run dev --workspace=apps/web",
    "web:start": "npm run start --workspace=apps/web",
    "db:migrate": "node scripts/migrate.js",
    "seed:demo": "node scripts/seed-demo.js"
  }
}